[ 你的业务代码 ]
       │
       ▼ (使用 DB 进行增删改查)
┌──────────────┐
│  GORM (*gorm.DB)   │ ── 负责：对象映射 (ORM)、拼装 SQL
└──────┬───────┘
       │ 内部持有 / 通过 DB.DB() 提取
       ▼
┌──────────────┐
│  database/sql│ ── 负责：连接池管理 (*sql.DB)、TCP 连接生命周期
└──────┬───────┘
       │
       ▼
  [ MySQL 数据库 ]